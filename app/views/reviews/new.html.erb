<h1>New review</h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: (0..5).to_a %>
  <%= f.submit class: 'mb-4' %>
<% end %>

<%= link_to 'Back to restaurant', restaurant_path(@restaurant.id), class: 'text-decoration-none' %>
